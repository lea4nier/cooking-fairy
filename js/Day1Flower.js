class Day1Flower extends Phaser.Scene {
    constructor() {
        super({ key: 'Day1Flower' });
    }

    preload() {
        // Preload your images for Day1Flower scene
        // this.load.image('flower1', 'assets/images/flower1.png');
        // this.load.image('flower2', 'assets/images/flower2.png');

        // this.load.image('flower3', 'assets/images/flower3.png');
    }

    create() {
        // const gameWidth = this.sys.game.config.width;
        // const gameHeight = this.sys.game.config.height;

        // const padding = 50;
        // const imageWidth = 400;
        // const imageHeight = 300;
        // const totalWidth = (imageWidth * 3) + (padding * 2);

        // const startX = (gameWidth - totalWidth) / 2;
        // const startY = gameHeight / 2 - imageHeight / 2;

        // this.add.image(startX, startY, 'flower1').setDisplaySize(imageWidth, imageHeight);
        // this.add.image(startX + imageWidth + padding, startY, 'flower2').setDisplaySize(imageWidth, imageHeight);
        // this.add.image(startX + 2 * (imageWidth + padding), startY, 'flower3').setDisplaySize(imageWidth, imageHeight);
    }
}


